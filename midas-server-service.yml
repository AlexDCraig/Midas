apiVersion: v1
kind: Service
metadata:
  name: midas-server-service
  labels:
    app: midas-server
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: midas-server
